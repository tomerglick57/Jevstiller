# The actions in .github/workflows are pinned by commit SHA; this keeps the pins current (one grouped PR a month).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
